import { Dimensions } from 'react-native'

const { height, width } = Dimensions.get('window')

export const APP_FULL_HEIGHT = height

export const APP_FULL_WIDTH = width

export const PICTURE_GRID_SIZE = width/3