import constants from 'src/redux/constants'

const initialState = {
	picture_url: null,
	thumbnail_url: null,
	uploading: false
}

export default (state = initialState, action) => {
	switch (action.type) {

	default:
		return state
	}
}